# Common dependencies
-r common.txt

numba == 0.65.0 # Required for N-gram speculative decoding

# Dependencies for NVIDIA GPUs on Jetson JP6 / CUDA 12.6 / Python 3.10 / aarch64
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0 # Required for phi3v processor. Must match torch version.
triton==3.4.0

# FlashInfer is optional.
# flashinfer-python==0.6.8.post1
# flashinfer-cubin==0.6.8.post1

apache-tvm-ffi==0.1.9
tilelang==0.1.9

nvidia-cudnn-frontend>=1.13.0,<1.19.0

fastsafetensors >= 0.2.2

nvidia-cutlass-dsl>=4.4.2
quack-kernels>=0.3.3
