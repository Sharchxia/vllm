# Common dependencies
-r common.txt

numba == 0.65.0 # Required for N-gram speculative decoding

# Installed separately from Jetson CUDA 12.6 index:
# torch==2.8.0
# torchaudio==2.8.0
# torchvision==0.23.0
# triton==3.4.0

# FlashInfer is optional and currently not matched to Python 3.10 Jetson wheel.
# flashinfer-python==0.6.8.post1
# flashinfer-cubin==0.6.8.post1

apache-tvm-ffi==0.1.9

# Disable first for Jetson baseline build.
# tilelang==0.1.9
# nvidia-cutlass-dsl>=4.4.2
# quack-kernels>=0.3.3

nvidia-cudnn-frontend>=1.13.0,<1.19.0
fastsafetensors >= 0.2.2
